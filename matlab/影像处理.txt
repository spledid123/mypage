digital image;read and show image;image arithmetic(四则运算)
(1)
types of digital image:binary(黑白)/grayscale(灰度)/rgb
read an image:i = imread();
show:imshow(i)/image(i)
imageinfo('')%图片的属性
imtool('')%比如看每个像素的rgb
(2)image processing
image arithmatic:
imabsdiff/imadd/imapplymatrix/imcomplement/imdivide/
imlincomb/immultiply/imsubtract
j = immultiply(i,1.5)//i.*1.5
imadd(i,j)//add
imhist(i);%make a histgram
histeq()%equalization 通过调色阶调高对比度，均衡
(3)geometric transformation
translation(平移)/scale(imresze())/shear(剪切)
/rotation(imratate())
*矩阵，线性代数
imwrite()%save the image


image thresholding;background estimation;connected-component labeling
(1)计算米粒个数：'rice.png'
二值化：binarize the image
calculate connected white pixels;
image thresholding：
graythresh()找出最佳临界灰度值/im2bw()image->binary image
（高于临界值黑，低于白）
but existing sparkles in the background and some grains missing
(2)estimate the background and subtract
background estimation:bg = imopen(I,strel('disk',15));
background subtraction:imsubtrac(I,bg);相减
(3)component computing计数-相邻像素标同一个数字
connected-component labeling:[labeled,numObjets]=bwlabel(bw,8);
->the max rice
(4)color-coding objects:
label2rgb(labeled)色彩计数
(5)object properties:regionprops(labeled,'basic')
每颗米的大小/中心点/上下左右界限
(6)interactive selection:ob = bwselect(bw);
单击，delete退出选择某个，双击结束
imshow(ob);
































