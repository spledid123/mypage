script writing;
structured programming
usert-defined fun
(1)matlab script(脚本) and editor
scripts :saved as a <file>.m
插入'fx'图标可以查找函数
'%'comment（注释），直接加/上方按钮
'%%':make section,有“运行节”按钮，只执行小节；
或者上方有按钮插入，节直到下一个%%之前停止（进入下一个节）
在某行前面按住加断点，breakpoint
这时候可以看工作区的变量数值debug
(需要退出调试)
全选右键->智能缩进
(2)structured programming
flow control:
if/elseif/else;for;switch/case/otherwise;try/catch;while
break/continue/end/pause/return
relational operator:
</<=/>/>=/==/~=(!=)/&&/||

if condition1 
	statement1;
elseif condition2
	statement2;
else 	
	statement3;
end
%  rem取余；disp=display；
switch expression//是一个数值
case value1 
	statement1;
..
otherwise 
	statement;
end
while expression
	statement;
end
for variable=start:increment:end
	commands;
end
(3)pre-allocating sapce to variable
例：A=zeros(2000,2000);
tic
...
toc计时
(4)some tips
before run:clear all/close all(closde all figures)/clc
use semicolon;
use '...' for line feeds/breaks(换行)
(5)functions
格式：function y = mean(x)
%...comment
//code
saved as mean.m
use .* so x/y can be a vector
//num2str/isempty
function-handle:f = @(x) exp(-2*x);
f(x)直接用
f is a point
@(x)表示句柄，





















