polynomial differentiation and integration(多项式微分积分)
numerial differentiation and integration（数值微分积分）
(1)represent polynomails
use row vector:f(x)=3x^3-2x-5->P=[3,0,-2,-5];
values of polynomail:f=polyval(P,x),P表示polynomial,x表示x，向量，f表示f(x)计算的数值,列向量,
polynomial differentiation:polyder(P);
return row vactor mean a polynomial;
polynomial integration,polyint(P,3),3 is C
(2)numeical differentiation/integration
differences:diff(x),calculate the differences betwwen adjacent
elements in the vactor x
numerical differentiation using diff()
integration:Σf(x)|xi+1-xi|,
积分方法：midpoint rule:用矩形，
eg.4x^3;h=0.05;x=0:h:2;midpoint=(x(1:end-1)+x(2:end))./2;y=4*midpoint.^3;sum(y.*h);
trapezoid rule:用梯形：
h=0.05;x=0:h:2;y=4*x.^3;s=h*trapz(y);%专门梯形算积分的
simpson's rule:辛普森方法；
(3)@function handles
@fun is a point of fun
function [y] = xy_plot(input,x)
y = input(x);plot(x,y);
end
xy_plot(@sin,1:0.01:2);
(4)numerical integration
y = @(x) x.^3;
integral(y,0,2);%算积分
%必须用点乘
double /triple integral:integral2/integral3
//使用sym变量
diff(y,n)求导，int(y,[a,b])求积分
