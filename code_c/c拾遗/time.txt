#include<stdio.h>
#include<stdlib.h>
#include<time.h>
//#include<>

int main(){
	char a[10],c[10],d[10]="ios5love";
	int b=10;
	sprintf(a,"%o",b);//格式化数据输入字符串
//    fprintf(stdout,"%s",a);//格式化输出到输出流 
    
      time_t second;
    //second = time(NULL);//
     time(&second);//获取时间/秒数 
    printf("%d",second);
    struct tm *p = localtime(&second);
	printf("\n%d",p->tm_sec); 
	
} 
//double difftime(time_t time1, time_t time2)返回两时间的差值（秒
//char *ctime(&second);//返回字符串Thu Jun 25 09:41:30 2020
/*struct tm
{
           int tm_sec;     /* 秒，范围为 0~59，60 的话表示闰秒 *// 
//           int tm_min;     /* 分，范围是 0~59 * 
//           int tm_hour;    /* 时，范围是 0~23 */
//           int tm_mday;    /* 一个月中的第几天，范围是 1~31 */
//           int tm_mon;     /* 一年中的第几个月，范围是 0~11 */
//           int tm_year;    /* 自 1900 年往后的第几年 */
//           int tm_wday;    /* 星期几，自星期天开始计算，范围是 0~6 */
//           int tm_yday;    /* 一年中的第几天，范围是 0~365 */
//           int tm_isdst;   /* 指定日光节约时间是否生效，正数表示生效，0 表示不生效，负数表示该信息不可用 */
//};
 
