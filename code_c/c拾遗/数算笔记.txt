<string.h>
int strlen(char *s);
复制char *strcpy(char destin[],char*source);返回destin，高度依赖source的'\0'//destin只能为数组指针
拼接char *strcat(char *destin,char*source);返回destin（*destin=destin+source）
比较 int ctrcmp(char *str1,char *str2);看Asic码，str1>str2，返回值 > 0；两串相等，返回0
定位char*strchr(char*str,char c);首次出现c的位置
子串：char *strstr(const char *str1,const char *str2);
将串转化为double型double strtod(char *ctr,char**endptr)
分割char *strtok(char a[],const char *b)，多次使用，用NULL


sscanf不支持‘ ’作为%s的分隔符
a[10]qsort（a,10,sizeof(int),comp）comp表示比较函数
int comp(const void *a, const void *b) { return *(int *)a - *(int *)b; }<0a在前


