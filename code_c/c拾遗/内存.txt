#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main(){
	int *a;
	a = (int *)malloc(sizeof(int)*10);//a=(int *)calloc(10,sizeof(int));等价
				//a=realloc(a,44);//改变a占用的内存空间
				//free(a);释放
	int *b;
	b = (int *)malloc(sizeof(int)*10);
//<string.h>memset(a,0,sizeof(int)*10);//(内存空间指针，填充值，填充前多少字节)
	//给a后的多少个字节赋值，所以只能用0/\0 
	a[0]=1;a[7]=7;
	memmove(b,a,40);//复制：(被传递内存指针，原内存指针，字节数)；
	printf("a[0]=%d,b[0]=%d",a[0],b[0]); 
	printf("\n比较：%d",memcmp(a,b,40));//逐字节进行比较
	int flag=7;
	if (memchr (a,flag,40)){
		printf("success");
	} 
}
