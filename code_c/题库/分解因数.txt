#include<stdio.h>
int main()
{
	int find(int m,int t);
	int n,i,suu,gongjushu;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&gongjushu);
		suu=find(gongjushu,1)+1;
		printf("%d\n",suu);
	}
	return 0;
}
int find(int m,int t)
{
	int i;
    int num=0;
	for(i=t;i*i<=m;i++)
	{
		if(m%i==0)
		{
		   num+=find(m/i,i)+1;
		}
	}
	return(num);
}
